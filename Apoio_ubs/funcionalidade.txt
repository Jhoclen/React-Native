Tela 1: Escolha do Serviço	Lista simples dos serviços (FlatList).
 Ao clicar, navega para a próxima tela.	FlatList (para performance de listagem)
  e TouchableOpacity para o clique.

Tela 2: Seleção de Data e Hora	Exibir apenas os slots de horário onde disponivel:
 true da sua lista fictícia.	Componentes básicos de View e Text.
  Você pode usar o $DateTimePicker se quiser um seletor de calendário mais visual.

Tela 3: Pré-Triagem e Confirmação	Um formulário simples (inputs de texto e botões de rádio)
. O usuário insere o nome (fictício) e as respostas da triagem.	TextInput (para o nome)
 e RadioButton (para o questionário, ex: "Está com febre? Sim/Não").



 import { Calendar, Syringe, MapPin, BookOpen, Bell } from "lucide-react";


import { FlatList, TouchableOpacity } from "react-native"
import { Text } from "../components/Text";
import { agenda } from "../../mocks/agenda"
import { Container } from "./styles";

export default function Services(){
    return(
        <Container>
            <Text>
                Nossos Serviços
            </Text>
        </Container>
    )

}


import styled from "styled-components/native";

export const Container = styled.View`
    margin: 24px 24px 0px;

`;